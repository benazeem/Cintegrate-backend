version: "3.9"

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: backend-prod

    ports:
      - "4000:4000"

    env_file:
      - .env

    restart: always